{
  "type": "changed",
  "message": "The .NET client now snapshots caller-owned configuration, freezes serializer options, enforces valid `StringOrMarkdown` values, and consistently uses value-semantic `HostId` APIs.",
  "targets": ["dotnet"]
}
