{
  "id": "transport.framing.edge.complete-messages",
  "behaviorIds": [
    "transport.framing.edge.complete-messages"
  ],
  "description": "Negative/error-path: Splitting an action JSON across two frames must not be accepted as valid; host must reject or close. Client surfaces JSON-RPC error code -32601.",
  "protocolVersion": "0.3.0",
  "notes": "Auto-generated from D7 negative paths: transport.framing.edge.complete-messages. Transport MUST deliver complete messages (no partial delivery). Each WebSocket text frame contains exactly one complete JSON-RPC message.",
  "steps": [
    {
      "op": "client.request",
      "label": "send reconnect to trigger the error condition",
      "method": "reconnect",
      "params": {
        "channel": "ahp-session:/test",
        "lastSeenServerSeq": 0
      },
      "id": 1
    },
    {
      "op": "server.response",
      "label": "server returns error code -32601",
      "forId": 1,
      "error": {
        "code": -32601,
        "message": "Error: transport-framing"
      }
    },
    {
      "op": "client.assert.error",
      "label": "client surfaces error code -32601",
      "code": -32601
    }
  ]
}
