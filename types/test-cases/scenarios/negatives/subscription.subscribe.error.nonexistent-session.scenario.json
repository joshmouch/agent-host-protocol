{
  "id": "subscription.subscribe.error.nonexistent-session",
  "behaviorIds": [
    "subscription.subscribe.error.nonexistent-session"
  ],
  "description": "Negative/error-path: subscribe{channel:\"ahp-session:/00000000-0000-0000-0000-000000000000\"} → error.code===-32001. Client surfaces JSON-RPC error code -32001.",
  "protocolVersion": "0.3.0",
  "notes": "Auto-generated from D7 negative paths: subscription.subscribe.error.nonexistent-session. subscribe to ahp-session:/<nonexistent-uuid> should return SessionNotFound (-32001).",
  "steps": [
    {
      "op": "client.request",
      "label": "send subscribe to trigger the error condition",
      "method": "subscribe",
      "params": {
        "channel": "ahp-session:/00000000-0000-0000-0000-000000000000"
      },
      "id": 1
    },
    {
      "op": "server.response",
      "label": "server returns error code -32001",
      "forId": 1,
      "error": {
        "code": -32001,
        "message": "Error: error:SessionNotFound"
      }
    },
    {
      "op": "client.assert.error",
      "label": "client surfaces error code -32001",
      "code": -32001
    }
  ]
}
