# D11 — Reconciliation: canonical-key coverage (corrects the raw matrix)

> Generated by `conformance/discovery/scripts/reconcile.mjs` (deterministic, no
> LLM judgment). The raw `d11-surface-matrix.{jsonl,md}` merges by literal
> `behavior-id` and so under-collapses: independent angles named the same
> behavior differently, so only **4 of 656** rows merged and the exit-criterion
> "PASS" there is *circular* (every D1 element is "mapped" only because D1 itself
> emitted it). This file imposes a canonical key —
> `strip StateAction:/error: → drop non-alphanumerics → lowercase` — and
> measures the REAL coverage question.

## Headline (honest) numbers

| Metric | Value |
|---|---:|
| Total angle rows (D1–D10) | 656 |
| Raw `behavior-id` matrix lines | 652 |
| **Distinct canonical concepts** | **384** |
| D1 schema elements (denominator) | 122 |
| — touched by a scenario-producing angle | **90 (73.8%)** |
| — NOT touched by any scenario angle | 32 |
| D2 strong-normative clauses (MUST/MUST_NOT/REQUIRED/SHALL) | 30 |
| — touched by a scenario-producing angle | **11 (36.7%)** |
| — NOT touched by any scenario angle | 19 |

"Touched by a scenario-producing angle" = the same canonical concept appears in
at least one of D3/D4/D5/D6/D7/D8/D9/D10 (the angles that turn into runnable
checks), not just in its own denominator file.

## What this means for Part 1's exit criterion

The plan's exit bar is: *every D1 surface element and every D2 MUST/REQUIRED
clause is mapped to a planned scenario or explicitly out-of-scope-with-reason.*
The **honest** status:

- **Enumeration is complete and verified** — 656 behaviors, every one
  shape-valid and citation-grounded (both gates re-run independently).
- **Reconciliation is partial** — 90/122 schema elements
  and 11/30 strong-normative clauses are currently
  corroborated by a scenario-producing angle. The remaining
  32 schema elements + 19 MUST-class
  clauses are enumerated but **not yet matched to a scenario angle** — they are
  the precise Part-2 authoring backlog (and the honest gap the raw matrix hid).

## D1 schema elements NOT yet touched by a scenario angle (32)

These are the schema-typed surfaces no D3–D10 angle exercised. Most are
`StateAction` variants and channel notifications a Part-2 fixture/scenario must
drive:

- `ActionEnvelope`
- `OtlpExportLogs`
- `OtlpExportMetrics`
- `OtlpExportTraces`
- `SessionAdded`
- `SessionRemoved`
- `SessionSummaryChanged`
- `Snapshot`
- `StateAction:SessionToolCallApproved`
- `StateAction:SessionToolCallDenied`
- `TelemetryCapabilities`
- `changeset-channel`
- `completions`
- `createResourceWatch`
- `createTerminal`
- `disposeTerminal`
- `fetchTurns`
- `invokeChangesetOperation`
- `protocolVersion`
- `reconnect-resnapshot`
- `resolveSessionConfig`
- `resourceCopy`
- `resourceDelete`
- `resourceList`
- `resourceMkdir`
- `resourceMove`
- `resourceRequest`
- `resourceResolve`
- `root-channel`
- `session-channel`
- `sessionConfigCompletions`
- `terminal-channel`

## D2 strong-normative clauses NOT yet touched by a scenario angle (19)

- **MUST** `protocol-version` — lifecycle.protocol-version.version.protocolversion-is-t
- **MUST** `error-response` — lifecycle.error-response.error.if-the-server-cannot
- **MUST** `rpc` — rpc.rpc.happy.a-json-rpc-request-h
- **MUST_NOT** `notification` — rpc.notification.edge.a-json-rpc-notificat
- **MUST** `createResourceWatch` — channel.createResourceWatch.happy.the-id-is-receiver-a
- **MUST** `createResourceWatch` — channel.createResourceWatch.error.the-receiver-must-ga
- **MUST** `channel` — channel.channel.edge.emitted-when-any-mut
- **MUST** `channel` — session.channel.error.when-the-server-rece
- **MUST** `channel` — session.channel.happy.any-action-referenci
- **MUST** `session-action-toolCallConfirmed` — session.session-action-toolCallConfirmed.error.session-toolcallconf
- **MUST** `session-action-turnCancelled` — session.session-action-turnCancelled.error.session-turncancelle
- **MUST** `session-action-modelChanged` — session.session-action-modelChanged.happy.session-modelchanged
- **MUST** `session-action-agentChanged` — session.session-action-agentChanged.happy.session-agentchanged
- **MUST** `auth` — subscription.auth.concurrency.ahp-otlp-authority-p
- **MUST_NOT** `terminal` — channel.terminal.happy.the-server-must-not
- **MUST** `transport` — transport.transport.happy.a-compliant-transpor
- **MUST** `versioning` — versioning.versioning.version.both-peers-must-use
- **MUST** `versioning` — versioning.versioning.version.within-a-compatible
- **MUST_NOT** `versioning` — versioning.versioning.version.3-on-a-successful-ne

## Reproduce

```bash
node conformance/discovery/scripts/reconcile.mjs
cat conformance/discovery/out/d11-coverage.json
```
