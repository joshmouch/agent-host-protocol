# Stryker dev-only deps — installed locally / in CI, never committed.
node_modules/

# Stryker sandbox + generated reports — regenerated by `npm run mutate`.
.stryker-tmp/
reports/
stryker.log

# pin lockfile is fine to keep, but the reports are large generated artifacts.
